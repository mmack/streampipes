apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "streampipes.fullname" . }}-test-connection"
  labels:
    {{- include "streampipes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "streampipes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
